import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container:{
        flex:1,
        width: '100%',
    },
    thumbvideo:{
        width: '100%',
        height: '100%'
    },
    videoPlayer:{
        // flex:1,
        // width: '90%'
    }
});

export default styles;
