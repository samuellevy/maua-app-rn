function titlePrimary(){
    const styles = StyleSheet.create({
        ruleTitle:{
            fontSize: 14,
            fontWeight: 'bold',
            color: colors.primary,
            paddingBottom: 10,
            lineHeight: 16,
        },
    });
}